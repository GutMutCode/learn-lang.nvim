-- These are executed when Neovim starts.
-- This is useful for setting up keymaps or autocommands that should be available regardless of whether the user requires the plugin or not.
print("plugin/example.lua is executed!")
